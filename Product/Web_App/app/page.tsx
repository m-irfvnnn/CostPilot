'use client'

import { useState } from 'react'
import Link from 'next/link'
import { ArrowRight, BarChart3, Check, CircleDollarSign, Menu, Radar, ShieldCheck, Sparkles, TrendingUp, X, Zap } from 'lucide-react'
import { BILLING_PLAN_LIST } from '@/lib/billing-plans'
import { buildPlanHref } from '@/lib/demo-plan-intent'

const nav = [['Product', '#product'], ['How it works', '#how-it-works'], ['Pricing', '#pricing']]
const values = [
  ['Track', 'See which agents, workflows, runs, and models drive spend.', 'Bring token usage, API calls, providers, and workloads into one clear view.', Radar],
  ['Forecast', 'Understand projected AI spend before the bill arrives.', 'Use run rates and workflow patterns to plan with confidence, not hindsight.', TrendingUp],
  ['Control', 'Catch anomalies and manage budget thresholds.', 'Set guardrails that keep AI teams aligned to cost, performance, and usage goals.', ShieldCheck],
] as const
const steps = [['01', 'Connect', 'Connect AI providers, gateways, and workflow tools that generate usage.'], ['02', 'Monitor', 'Track workflow runs, model usage, tokens, costs, and budgets.'], ['03', 'Optimize', 'Identify cost drivers and prioritize the changes that reduce waste.']]
const cases = [['Engineering / AI teams', 'Understand which agents, workflows, models, and runs are driving spend.', BarChart3], ['FinOps / Finance', 'Forecast AI spend and keep budgets predictable before invoices arrive.', CircleDollarSign], ['Operations / RevOps', 'Spot unusual usage patterns and route the right follow-up action.', Zap]] as const
const plans = BILLING_PLAN_LIST.map((plan) => ({
  id: plan.id,
  name: plan.label.replace('CostPilot ', ''),
  price: plan.id === 'growth' ? '$99' : plan.id === 'starter' ? '$0' : 'Custom',
  desc: plan.description,
  cta: plan.cta,
  featured: plan.featured,
  perks: plan.perks,
}))

function Logo() {
  return <a href="#top" className="flex items-center gap-2.5"><span className="grid h-8 w-8 place-items-center rounded-lg bg-primary text-primary-foreground"><Sparkles size={16} /></span><span className="font-heading text-lg font-bold tracking-tight">CostPilot</span></a>
}

function Dashboard() {
  const bars = [42, 49, 39, 61, 58, 72, 66, 81, 75, 91, 84, 96]
  return <div className="relative w-full max-w-[610px] lg:ml-auto"><div className="absolute -inset-6 rounded-[2rem] bg-gradient-to-br from-[#4355FE]/12 via-transparent to-[#96EFEF]/20 blur-2xl" /><div className="relative overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-[0_24px_70px_-32px_rgba(67,85,254,.35)]"><div className="flex items-center justify-between border-b border-slate-100 px-5 py-4"><span className="flex items-center gap-2 font-mono text-[11px] font-bold uppercase tracking-[.16em] text-slate-500"><i className="h-2 w-2 rounded-full bg-emerald-500" />Workflow cost overview</span><span className="font-mono text-[10px] text-slate-400">LAST 30 DAYS</span></div><div className="p-5 sm:p-6"><div className="mb-6 flex items-end justify-between"><div><p className="font-mono text-[10px] uppercase tracking-[.16em] text-slate-400">AI workflow spend</p><p className="mt-1 font-heading text-3xl font-bold text-slate-900">$24,820</p><p className="mt-1 font-mono text-[11px] text-emerald-600">1,248 runs <span className="text-slate-400">across 12 workflows</span></p></div><span className="demo-label">DEMO DATA</span></div><div className="grid grid-cols-3 gap-2.5">{[['Cost / run', '$19.88', 'avg workflow cost'], ['Projected spend', '$31,420', 'by month end'], ['Budget risk', '82.7%', '5 days remaining']].map(([label, value, note]) => <div key={label} className="rounded-xl border border-slate-100 bg-slate-50/70 p-3"><p className="font-mono text-[9px] uppercase tracking-[.12em] text-slate-400">{label}</p><p className="mt-2 font-heading text-base font-bold text-slate-800">{value}</p><p className="mt-1 font-mono text-[9px] text-slate-400">{note}</p></div>)}</div><div className="mt-6 rounded-xl border border-slate-100 p-4"><div className="mb-5 flex justify-between"><div><p className="font-mono text-[10px] uppercase tracking-[.14em] text-slate-400">Model usage trend</p><p className="mt-1 font-mono text-xs text-slate-600">DeepSeek · OpenAI · Anthropic</p></div><span className="font-mono text-[10px] text-primary">$6.8k savings</span></div><div className="flex h-24 items-end gap-1.5">{bars.map((height, index) => <div key={index} className={`flex-1 rounded-sm ${index > 8 ? 'bg-primary' : 'bg-[#96EFEF]'}`} style={{ height: `${height}%` }} />)}</div></div><div className="mt-6 flex justify-between border-t border-slate-100 pt-4 font-mono text-[10px]"><span className="flex items-center gap-2 text-slate-500"><Zap size={13} className="text-primary" />Optimization opportunity</span><b className="text-amber-600">3 actions found</b></div></div></div></div>
}

function pricingHref(planId: 'starter' | 'growth' | 'scale') {
  if (planId === 'scale') return buildPlanHref('/contact-sales', 'scale')
  return buildPlanHref('/signup', planId)
}

export default function Page() {
  const [open, setOpen] = useState(false)
  return <main id="top" className="min-h-screen overflow-hidden bg-background"><header className="border-b border-slate-200/80"><div className="page-shell flex h-[72px] items-center justify-between"><Logo /><nav className="hidden items-center gap-8 md:flex">{nav.map(([label, href]) => <a key={label} href={href} className="font-mono text-xs text-slate-500 hover:text-primary">{label}</a>)}</nav><div className="hidden items-center gap-5 md:flex"><Link href="/login" className="font-mono text-xs text-slate-500">Log in</Link><Link href="/signup" className="button-primary">Start Free <ArrowRight size={14} /></Link></div><button className="p-2 md:hidden" onClick={() => setOpen(!open)} aria-label={open ? 'Close menu' : 'Open menu'}>{open ? <X /> : <Menu />}</button></div>{open && <div className="border-t border-slate-200 px-5 py-5 md:hidden"><div className="flex flex-col gap-5">{nav.map(([label, href]) => <a key={label} href={href} className="font-mono text-sm text-slate-600">{label}</a>)}<Link href="/login" className="font-mono text-sm text-slate-600">Log in</Link><Link href="/signup" className="button-primary w-fit">Start Free <ArrowRight size={14} /></Link></div></div>}</header><section className="relative"><div className="absolute right-0 top-0 -z-0 h-[480px] w-[55%] bg-gradient-to-bl from-[#96EFEF]/25 via-[#5572F9]/8 to-transparent blur-3xl" /><div className="page-shell relative z-10 grid items-center gap-14 py-20 lg:grid-cols-[.9fr_1.1fr] lg:gap-16 lg:py-32"><div><p className="eyebrow"><i className="h-1.5 w-1.5 rounded-full bg-primary" />AI WORKFLOW COST INTELLIGENCE</p><h1 className="mt-6 max-w-xl font-heading text-5xl font-bold leading-[1.04] tracking-[-.045em] text-slate-950 sm:text-6xl">Control agentic workflow costs <span className="hero-gradient">before they surprise you.</span></h1><p className="mt-7 max-w-lg font-mono text-sm leading-7 text-slate-500">CostPilot helps teams understand AI agents, workflow runs, model/API usage, spend, budgets, forecasts, alerts, and optimization opportunities in one recruiter-ready demo workspace.</p><div className="mt-9 flex flex-col gap-3 sm:flex-row"><Link href="/signup" className="button-primary justify-center">Start Free — No Credit Card <ArrowRight size={15} /></Link><Link href="/demo" className="button-secondary justify-center">View Demo Dashboard</Link></div><p className="mt-5 font-mono text-[10px] uppercase tracking-[.12em] text-slate-400">No setup fees · Synthetic demo workspace included</p></div><Dashboard /></div></section><section id="product" className="section-border bg-white"><div className="page-shell py-20 md:py-28"><p className="eyebrow">THE COSTPILOT DIFFERENCE</p><h2 className="section-title mt-5 max-w-xl">Workflow visibility before optimization.</h2><div className="mt-12 grid gap-5 md:grid-cols-3">{values.map(([eyebrow, title, detail, Icon]) => <article key={eyebrow} className="value-card"><div className="mb-10 flex justify-between"><span className="grid h-10 w-10 place-items-center rounded-lg bg-[#96EFEF]/40 text-primary"><Icon size={19} /></span><span className="font-mono text-[10px] uppercase tracking-[.16em] text-slate-400">{eyebrow}</span></div><h3 className="font-heading text-xl font-bold text-slate-900">{title}</h3><p className="mt-4 font-mono text-xs leading-6 text-slate-500">{detail}</p></article>)}</div></div></section><section id="how-it-works" className="section-border"><div className="page-shell py-20 md:py-28"><p className="eyebrow">A CLEARER WAY FORWARD</p><h2 className="section-title mt-5 max-w-md">From raw model usage to confident cost decisions.</h2><div className="mt-14 grid border-y border-slate-200 md:grid-cols-3">{steps.map(([number, title, text], index) => <article key={number} className={`py-8 md:px-7 md:py-10 ${index ? 'border-t border-slate-200 md:border-l md:border-t-0' : ''}`}><span className="font-mono text-xs font-bold text-primary">{number}</span><h3 className="mt-10 font-heading text-2xl font-bold text-slate-900">{title}</h3><p className="mt-3 max-w-xs font-mono text-xs leading-6 text-slate-500">{text}</p></article>)}</div></div></section><section className="section-border bg-white"><div className="page-shell py-20 md:py-28"><p className="eyebrow">BUILT FOR THE WHOLE TEAM</p><h2 className="section-title mt-5 max-w-lg">One source of truth for every AI cost conversation.</h2><div className="mt-12 grid gap-5 md:grid-cols-3">{cases.map(([title, text, Icon]) => <article key={title} className="use-case"><Icon size={18} className="text-primary" /><h3 className="mt-8 font-heading text-lg font-bold text-slate-900">{title}</h3><p className="mt-3 font-mono text-xs leading-6 text-slate-500">{text}</p></article>)}</div></div></section><section id="pricing" className="section-border"><div className="page-shell py-20 md:py-28"><div className="text-center"><p className="eyebrow justify-center">SIMPLE, TRANSPARENT PRICING</p><h2 className="section-title mt-5">Start with clarity.</h2><p className="mt-4 font-mono text-xs text-slate-500">Pricing shown for demonstration purposes.</p></div><div className="mx-auto mt-12 grid max-w-5xl gap-5 md:grid-cols-3">{plans.map(plan => <article key={plan.name} className={`pricing-card ${plan.featured ? 'pricing-featured' : ''}`}><div className="flex justify-between"><h3 className="font-heading text-lg font-bold">{plan.name}</h3>{plan.featured && <span className="rounded-full bg-[#96EFEF]/50 px-2.5 py-1 font-mono text-[9px] font-bold uppercase text-primary">Popular</span>}</div><p className="mt-8 font-heading text-4xl font-bold tracking-tight">{plan.price}{plan.name === 'Growth' && <small className="font-mono text-xs font-normal text-slate-400"> / month</small>}</p><p className="mt-3 min-h-10 font-mono text-xs leading-5 text-slate-500">{plan.desc}</p><Link href={pricingHref(plan.id)} className={`${plan.featured ? 'button-primary' : 'button-secondary'} mt-8 w-full justify-center`}>{plan.cta} <ArrowRight size={14} /></Link><ul className="mt-8 space-y-3 border-t border-current/10 pt-6">{plan.perks.map(perk => <li key={perk} className="flex items-center gap-2 font-mono text-[11px] text-slate-500"><Check size={13} className="text-emerald-500" />{perk}</li>)}</ul></article>)}</div></div></section><section className="px-4 py-8 sm:px-6 md:py-12"><div className="page-shell rounded-2xl bg-primary px-6 py-14 text-center sm:px-12 md:py-20"><p className="font-mono text-[10px] font-bold uppercase tracking-[.2em] text-[#99F7F0]">READY WHEN YOU ARE</p><h2 className="mx-auto mt-5 max-w-2xl font-heading text-4xl font-bold text-white sm:text-5xl">Make AI workflow costs predictable.</h2><p className="mx-auto mt-5 max-w-md font-mono text-sm leading-6 text-white/70">Start exploring CostPilot with a synthetic demo workspace.</p><Link href="/signup" className="button-light mt-8">Start Free — No Credit Card <ArrowRight size={15} /></Link></div></section><footer className="border-t border-slate-200"><div className="page-shell flex flex-col gap-7 py-8 sm:flex-row sm:items-center sm:justify-between"><Logo /><nav className="flex flex-wrap gap-x-6 gap-y-3">{[...nav, ['Login', '/login'], ['Start Free', '#pricing']].map(([label, href]) => <a key={label} href={href} className="font-mono text-[10px] uppercase tracking-[.1em] text-slate-500 hover:text-primary">{label}</a>)}</nav></div></footer></main>
}
